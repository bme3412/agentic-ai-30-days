{
  "name": "gen-ai-30-days-web-app",
  "version": "1.0.0",
  "description": "30 Days of Agentic AI learning platform",
  "type": "module",
  "scripts": {
    "build": "esbuild src/app.ts --bundle --outfile=dist/app.js --format=esm --target=es2020",
    "dev": "esbuild src/app.ts --bundle --outfile=dist/app.js --format=esm --target=es2020 --watch --servedir=.",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.0"
  }
}
